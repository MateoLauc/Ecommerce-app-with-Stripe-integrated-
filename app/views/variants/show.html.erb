	    <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header" style="margin-left:100px;">
      <a class="navbar-brand" href="/">ABSHOES</a>
    </div>
    <ul class="nav navbar-nav" style="margin-left:100px;">
      <% @categories.each do |c| %>
          <li><a href="<%= category_path(c)%>"><%=c.name%></a></li>
        <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right" style="margin-right:150px"> 
        <% if current_user %>

             <li> Logged in as <%= current_user.ime %>. </li>
             <li> <%= link_to "Log out", sessions_logout_path %> </li>
        <% else %>
           
              <li> <%= link_to "Sign up", users_new_path %> </li>
                <li> <%= link_to "Log in", sessions_login_path %> </li>
          <% end %>
      </ul>
  </div>
</nav>

		<div style="margin-top:50px">

			<div class="container group">

				<div class="row">
						
						<div class="col-md-6 col-xs-12">
							<%= image_tag @variant.product.image.url(:medium) %> 
						</div>
					
						<div class="col-md-6 col-xs-12">
							<h3 style="font-weigth:bold"><%=@variant.product.naziv %></h3><br>
							<p><%= @variant.product.opis%></p>
								<div class="col-md-4" name="velicina" style="padding:0px;">Velicina:</br>
										<select class="selectpicker" form="korpa" style="width:180px;">
						                    <option>43</option>
						                    <option>44 </option>
						                    <option>45 </option>
						                </select>
					            </div>

					            <div class="col-md-5" style="float:left; padding:0px;" >
					                	Kolicina: 
					                <input value="1" name="qty" form="korpa" style="width:70px;display:block" >
													
					            </div>
								<div id="dugme" class="col-md-6" style="padding:10px 0px">
									<%= form_tag line_items_path(:variant_id => @variant), method: :post , id: "korpa" do%>
									 
									<%= submit_tag "DODAJ U KORPU"%>
								</div>
								<%end%>	
						</div>
				</div>

			<div class="text-center" style="margin-top:100px;">
				<p>Slicni proizvodi </p>
			<div class="container" style="margin-top:50px;">
				<% @variants.each do |v| %>
    <div class="col-md-3 text-center">
    	<div class="element">
    		<span class="text"><p ><%=v.product.naziv %></p>
    		<p>&euro;<%=v.price  %> </p> </span>
     		 <a href="<%= variant_path(v)%>"> <%= image_tag v.product.image.url(:square) %> </a>
       <a href="<%= variant_path(v)%>"><div class="hov" ></div></a>
    			
      </div>
    </div>
<% end %>
			</div>
			</div>
			</div>
		</div>



<div class="panel-footer" style="margin-top:150px ">
			<div class="container">
				<div class="row"></div>		
					<div class="col-md-4">
						<p style="color:#999; font-weight:400px; font-size:12px">
							Copyright&copy;ABHShoes.All rights reserved.
						</p>
					</div>
					<div class="col-md-8">
						<ul class="footer-nav">
						
								 <% @categories.each do |c| %>
					         		 <li>
					         		 	<a href="<%= category_path(c)%>"><%=c.name%></a></li>
					        <% end %>	
						</ul>
					</div>
			</div>
		</div>
	</div>
</div>
