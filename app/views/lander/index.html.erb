<div style="text-align:center">
<h1 >Lander page</h1>
  <% if current_user %>
    Logged in as <%= current_user.email %>.
    <%= link_to "Log out", sessions_logout_path %>
  <% else %>
    <%= link_to "Sign up", users_new_path %> or
    <%= link_to "Log in", sessions_login_path %>
  <% end %>
</div>
